# Viber Bot API - 555Mix

This project sends Viber broadcast messages using the D7 Networks API.

## Endpoints

- `/` → Check if API is running  
- `/send-message` → Sends a broadcast Viber message (POST)

Deployed via Render.com
